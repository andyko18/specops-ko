#!/usr/bin/env bash
# 음성 대조 픽스처 — 전부 안전한 관용구다. 룰이 여기서 매치하면 오탐이다.
# shellcheck disable=all
rm -rf ./build
rm -rf /tmp/literal-path
target="$1"
rm -rf "$target"
rm -rf "${target}"
eval "literal string"
eval 'single literal'
