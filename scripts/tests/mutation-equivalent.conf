# target|line|pattern|reason — 알려진 equivalent mutant (분모 제외). return-code/관찰불가 변형만.
# governance-lib: caller 가 result=$(func ... || true) 로 stdout만 캡처·종료코드 무시 → return 0 변형 무관찰(equivalent)
# pattern 컬럼 필수 — L1600/L1608 은 'return 0'+'&&' 두 변형, return 0 만 제외(&&는 행동기반 killed 유지)
# line 컬럼은 절대 줄번호다 — target 이 자라면 조용히 어긋난다. `mutation-score.sh --check-conf` 로 1초 만에 확인한다.
# ★ 20260912 재정렬: file-class SoT 도입이 governance-lib 을 1558→1618 로 늘려 17건이 stale 이 됐다.
#   균일 오프셋이 아니라 구간별이라(삽입 4곳 +92 · _files_all_docs 축소 −24) **소속 함수 대조**로 재매핑했다.
# ★ 20260912 2차 재정렬(Phase C I-1): def fc_is_code 사본 2개를 지우고 _FC_JQ_DEF 상수를 앞에 넣어
#   **부호가 구간마다 반대**가 됐다 — 앞 구간 +31(상수 삽입, detect_fid 90→121),
#   log_friction 이후 전 구간 −12(사본 제거). 오프셋을 쓰되 **BASE↔HEAD 원문 문장을 대조해**
#   확정했다(11건 문자 단위 일치). 오프셋만 믿으면 틀린다 — 1차 재정렬 때 18건 중 1건만 맞았다.
#
# ⚠️ 이 conf 의 진짜 위험은 stale 이 아니라 **무음 오매칭**이다. 줄이 밀린 자리에 마침 다른
#   `return 0` 이 오면 --check-conf 가 통과해버리고, 그러면 **엉뚱한 가드가 등가로 제외**된다.
#   이번에 2건 실제로 발생했다:
#     · verification-state|113 — 원래 WAIVED 만료메타(→120)인데 HEAD 113 의 비정규 verdict 분기와 매칭
#     · governance-lib|1125 — 원래 load_rules(→1113)인데 HEAD 1125 의 apply_lookback_rule 과 매칭
#   둘 다 STALE 목록에 **안 뜬 채로** 잘못 가리키고 있었다. 재정렬 시 stale 만 고치지 말고
#   **전 항목의 reason ↔ 소속 함수**를 대조할 것. (근본 해법은 줄번호 키를 앵커 문자열로 바꾸는 것 — 별도 FID)
hooks/governance-lib.sh|121|return 0|stdout-contract: detect_fid graceful return
hooks/governance-lib.sh|1008|return 0|stdout-contract: read_recent_tool_events graceful return
hooks/governance-lib.sh|1060|return 0|stdout-contract: log_friction dedup return
hooks/governance-lib.sh|1081|return 0|stdout-contract: log_friction_sev 빈 fid return
hooks/governance-lib.sh|1087|return 0|stdout-contract: log_friction_sev mkdir 실패 return
hooks/governance-lib.sh|1098|return 0|stdout-contract: log_friction_sev dedup return
hooks/governance-lib.sh|1113|return 0|stdout-contract: load_rules graceful return
hooks/governance-lib.sh|1125|return 0|stdout-contract: apply_lookback_rule graceful return
hooks/governance-lib.sh|1132|return 0|stdout-contract: apply_lookback_rule trigger 미매칭 return
hooks/governance-lib.sh|1310|return 0|stdout-contract: apply_skill_declaration_rule graceful return
hooks/governance-lib.sh|1368|return 0|stdout-contract: apply_assertion_without_test_rule transcript 부재 return
hooks/governance-lib.sh|1376|return 0|stdout-contract: apply_assertion_without_test_rule assertion 부재 return
hooks/governance-lib.sh|1447|return 0|stdout-contract: apply_advisor_section_rule transcript 부재 return
hooks/governance-lib.sh|1549|return 0|stdout-contract: apply_gbrain_absence_rule transcript 부재 return
hooks/governance-lib.sh|1559|return 0|stdout-contract: apply_gbrain_absence_rule verify 부재 return
hooks/governance-lib.sh|1586|return 0|stdout-contract: apply_gbrain_absence_rule evidence 부재 return
hooks/governance-lib.sh|1588|return 0|stdout-contract: apply_gbrain_absence_rule gbrain 존재 return (&& 변형은 미제외)
hooks/governance-lib.sh|1596|return 0|stdout-contract: apply_gbrain_absence_rule trivial-skip return (&& 변형은 미제외)
# ── verification-state (20260912-verify-stale-docs-scope) ────────────────────
# 아래 return 0 변형이 등가인 **실제 근거** (Phase B 재검토가 초판 주석의 오기를 적발 — 정정본):
#   ★ "종료코드를 보는 곳이 없다" 는 **거짓이었다**. 실제로는 governance-lib.sh:1265 `|| return 1` ·
#     reconcile-check.sh:131 · release-ready.sh:107 · run-verification.sh:48 이 CLI rc 를 본다.
#   등가가 성립하는 진짜 이유는 CLI 진입점의 형태다 — verification-state.sh 의
#     `vs::current "$fid"; printf '\n'` 에서 **후행 printf 가 함수 rc 를 가린다**(항상 0).
#     그리고 `vs::record` 에는 return 0 사이트가 없다. 따라서 아래 사이트의 rc 변형은
#     어떤 소비자에게도 도달하지 않는다.
#   ⚠️ 그 줄을 `vs::current "$fid" && printf '\n'` 류로 바꾸면 rc 가 새어나가고,
#     이 등가 등재가 **실 변이 6건을 숨기게 된다**. CLI 진입점을 고칠 때 이 블록을 함께 재검토할 것.
# ★ 20260912 3차 재정렬(Phase C 재판정 I-3 수정): toplevel 앵커가 신규 return 0 사이트를 만들었고
#   그 이후 전 사이트가 밀렸다. 그때 `|120|` 이 또 무음 오매칭이었다(WAIVED 만료메타 → 비정규 verdict).
# ★ 20260912 **4차** 재정렬(chain 코드리뷰 I-1·M-1): exclude pathspec 정정 주석 10줄 +
#   `--full-name` 주석 1줄이 들어가 **구간별로 +10 / +11** 로 갈렸다(균일 오프셋 아님).
#   `77→87` 만 +10, `99→110` 이후는 +11 — **사이트 목록을 순서대로 대조**해 확정했다.
#   네 번 연속 밀렸다는 사실 자체가 줄번호 키의 한계다(근본 해법은 앵커 문자열 전환 — 별도 FID).
#   ※ 위 근거는 CLI 진입점만 설명한다. **지문 사이트(67·70·87·110)의 실제 근거는 호출 형태**다 —
#     `vs::nondoc_fingerprint`·`vs::workspace_fingerprint` 호출 6곳이 전부 `$(…)` 캡처이고
#     (verification-state.sh:153·156·210·214 · pre-push:54 · record-task-receipt.sh:58,61 ·
#      check-task-receipt.sh:56,61 · run-all.sh:29), 소비자 어디에도 `set -e` 가 없다(실측 5파일 0건).
#     따라서 rc 변형이 어떤 관측점에도 도달하지 않는다. (chain 코드리뷰 M-4)
#     ※ 이 호출부 줄번호도 밀린다 — 숫자가 아니라 `$(vs::` 패턴으로 grep 할 것.
# ★ 등재하지 않은 것: L147(nondoc/tree 비교 조건)·L181·L183(WAIVED 필드 검증)의 `&&` 변형은
#   등가가 아니라 **실제 미커버**다 — 등가로 덮으면 자기발급 면제표가 된다. 테스트로 잡을 대상이다.
#   ※ 이 줄번호도 파일이 자라면 밀린다(방금 L133·L167·L169 에서 옮겼다 — chain 코드리뷰 M-2).
#     숫자가 아니라 **조건 내용**으로 찾을 것: STALE 판정 조건 결합 · WAIVED 필수 3필드 · 길이 상한.
scripts/_internal/verification-state.sh|42|return 0|stdout-contract: workspace_fingerprint NO_GIT 반환
scripts/_internal/verification-state.sh|45|return 0|stdout-contract: workspace_fingerprint mktemp 실패 NO_GIT
scripts/_internal/verification-state.sh|67|return 0|stdout-contract: nondoc_fingerprint NO_GIT 반환
scripts/_internal/verification-state.sh|70|return 0|stdout-contract: nondoc_fingerprint mktemp 실패 NO_GIT
scripts/_internal/verification-state.sh|87|return 0|stdout-contract: nondoc_fingerprint toplevel 판정 실패 NO_GIT (T3 신설 사이트)
scripts/_internal/verification-state.sh|110|return 0|stdout-contract: nondoc_fingerprint EMPTY 반환
scripts/_internal/verification-state.sh|116|return 0|stdout-contract: legacy_verdict evidence 부재 NOT_RUN
scripts/_internal/verification-state.sh|126|return 0|stdout-contract: current state 부재 legacy 위임
scripts/_internal/verification-state.sh|130|return 0|stdout-contract: current jq 실패 NOT_RUN
scripts/_internal/verification-state.sh|131|return 0|stdout-contract: current 비정규 verdict NOT_RUN
scripts/_internal/verification-state.sh|138|return 0|stdout-contract: current WAIVED 만료메타 부재 NOT_RUN
scripts/_internal/verification-state.sh|143|return 0|stdout-contract: current WAIVED 만료 NOT_RUN
scripts/_internal/verification-state.sh|160|return 0|stdout-contract: current STALE 반환
# ── pretool-governance (20260829-pretool-mutation-triage) ────────────────────
# ① rc-무시 return: 두 게이트 함수는 **bare 호출**이라(L137 _batch_pr_gate · L550
#    _release_ready_gate) 반환값이 어디에도 쓰이지 않는다. deny 는 return 이 아니라
#    `exit 0` 으로 즉시 나간다. 따라서 return 0→1 은 어떤 관측점에서도 구별되지 않는다.
#    (호출부를 직접 읽어 확인 — 추정 아님)
hooks/pretool-governance.sh|85|return 0|rc-ignored: _batch_pr_gate 는 L137 bare 호출, deny 는 exit 0
hooks/pretool-governance.sh|108|return 0|rc-ignored: _batch_pr_gate branch 미검출 조기반환
hooks/pretool-governance.sh|109|return 0|rc-ignored: _batch_pr_gate 빈 branch 조기반환
hooks/pretool-governance.sh|117|return 0|rc-ignored: _batch_pr_gate queue 부재 조기반환
hooks/pretool-governance.sh|118|return 0|rc-ignored: _batch_pr_gate docs-only 면제 반환
hooks/pretool-governance.sh|122|return 0|rc-ignored: _batch_pr_gate 비차단 판정 반환
hooks/pretool-governance.sh|467|return 0|rc-ignored: _release_ready_gate 는 L550 bare 호출
hooks/pretool-governance.sh|468|return 0|rc-ignored: _release_ready_gate 스크립트 부재 반환
hooks/pretool-governance.sh|509|return 0|rc-ignored: _release_ready_gate 대상 FID 0건 반환
hooks/pretool-governance.sh|531|return 0|rc-ignored: _release_ready_gate 전건 READY 반환
# ② 가드 중복: cd 실패가 `|| true` 로 삼켜지므로 -d 검사 유무가 상태를 바꾸지 않는다.
hooks/pretool-governance.sh|19|&&|redundant-guard: 다음 줄 `cd ... || true` 가 실패를 흡수
# ③ 관찰 동일: _rr_fid 가 비면 경로 `.specops//risk-profile.json` 이 존재할 수 없고, 파일이
#    없으면 jq 가 실패해 eff="" → strict 분기 미진입 = 원본과 동일 결과.
hooks/pretool-governance.sh|500|&&|observationally-equal: 파일 부재 시 eff 빈값 → strict 미진입
