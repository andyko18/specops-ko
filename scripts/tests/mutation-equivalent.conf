# target|line|pattern|reason — 알려진 equivalent mutant (분모 제외). return-code/관찰불가 변형만.
# governance-lib: caller 가 result=$(func ... || true) 로 stdout만 캡처·종료코드 무시 → return 0 변형 무관찰(equivalent)
# pattern 컬럼 필수 — L1258/L1266 은 'return 0'+'&&' 두 변형, return 0 만 제외(&&는 행동기반 killed 유지)
# line 컬럼은 절대 줄번호다 — target 이 자라면 조용히 어긋난다. `mutation-score.sh --check-conf` 로 1초 만에 확인한다.
hooks/governance-lib.sh|66|return 0|stdout-contract: detect_fid graceful return
hooks/governance-lib.sh|857|return 0|stdout-contract: read_recent_tool_events graceful return
hooks/governance-lib.sh|909|return 0|stdout-contract: log_friction dedup return
hooks/governance-lib.sh|930|return 0|stdout-contract: log_friction_sev 빈 fid return
hooks/governance-lib.sh|936|return 0|stdout-contract: log_friction_sev mkdir 실패 return
hooks/governance-lib.sh|947|return 0|stdout-contract: log_friction_sev dedup return
hooks/governance-lib.sh|962|return 0|stdout-contract: load_rules graceful return
hooks/governance-lib.sh|974|return 0|stdout-contract: apply_lookback_rule graceful return
hooks/governance-lib.sh|981|return 0|stdout-contract: apply_lookback_rule trigger 미매칭 return
hooks/governance-lib.sh|1080|return 0|stdout-contract: apply_skill_declaration_rule graceful return
hooks/governance-lib.sh|1138|return 0|stdout-contract: apply_assertion_without_test_rule transcript 부재 return
hooks/governance-lib.sh|1146|return 0|stdout-contract: apply_assertion_without_test_rule assertion 부재 return
hooks/governance-lib.sh|1146|return 0|stdout-contract: apply_advisor_section_rule transcript 부재 return
hooks/governance-lib.sh|1319|return 0|stdout-contract: apply_gbrain_absence_rule transcript 부재 return
hooks/governance-lib.sh|1329|return 0|stdout-contract: apply_gbrain_absence_rule verify 부재 return
hooks/governance-lib.sh|1356|return 0|stdout-contract: apply_gbrain_absence_rule evidence 부재 return
hooks/governance-lib.sh|1358|return 0|stdout-contract: apply_gbrain_absence_rule gbrain 존재 return (&& 변형은 미제외)
hooks/governance-lib.sh|1366|return 0|stdout-contract: apply_gbrain_absence_rule trivial-skip return (&& 변형은 미제외)
# ── pretool-governance (20260829-pretool-mutation-triage) ────────────────────
# ① rc-무시 return: 두 게이트 함수는 **bare 호출**이라(L137 _batch_pr_gate · L419
#    _release_ready_gate) 반환값이 어디에도 쓰이지 않는다. deny 는 return 이 아니라
#    `exit 0` 으로 즉시 나간다. 따라서 return 0→1 은 어떤 관측점에서도 구별되지 않는다.
#    (호출부를 직접 읽어 확인 — 추정 아님)
hooks/pretool-governance.sh|85|return 0|rc-ignored: _batch_pr_gate 는 L137 bare 호출, deny 는 exit 0
hooks/pretool-governance.sh|108|return 0|rc-ignored: _batch_pr_gate branch 미검출 조기반환
hooks/pretool-governance.sh|109|return 0|rc-ignored: _batch_pr_gate 빈 branch 조기반환
hooks/pretool-governance.sh|117|return 0|rc-ignored: _batch_pr_gate queue 부재 조기반환
hooks/pretool-governance.sh|118|return 0|rc-ignored: _batch_pr_gate docs-only 면제 반환
hooks/pretool-governance.sh|122|return 0|rc-ignored: _batch_pr_gate 비차단 판정 반환
hooks/pretool-governance.sh|336|return 0|rc-ignored: _release_ready_gate 는 L419 bare 호출
hooks/pretool-governance.sh|337|return 0|rc-ignored: _release_ready_gate 스크립트 부재 반환
hooks/pretool-governance.sh|378|return 0|rc-ignored: _release_ready_gate 대상 FID 0건 반환
hooks/pretool-governance.sh|400|return 0|rc-ignored: _release_ready_gate 전건 READY 반환
# ② 가드 중복: cd 실패가 `|| true` 로 삼켜지므로 -d 검사 유무가 상태를 바꾸지 않는다.
hooks/pretool-governance.sh|19|&&|redundant-guard: 다음 줄 `cd ... || true` 가 실패를 흡수
# ③ 관찰 동일: _rr_fid 가 비면 경로 `.specops//risk-profile.json` 이 존재할 수 없고, 파일이
#    없으면 jq 가 실패해 eff="" → strict 분기 미진입 = 원본과 동일 결과.
hooks/pretool-governance.sh|369|&&|observationally-equal: 파일 부재 시 eff 빈값 → strict 미진입
