---
name: start-batch
description: "[deprecated] /start-all 로 이름 변경 — 구 이름 alias (1~2 릴리즈 후 제거)"
triggers:
  - "/start-batch"
mode: ask
specops_version: 1.16.0
specops_layer: Lifecycle
reference_upstream: specops-auto-ko 독자 추가
---

# /start-batch (deprecated → /start-all)

> **이 슬래시는 `/start-all` 로 이름이 바뀌었습니다.** 동작은 동일(requirements.md FR 표 전체 일괄 3-Phase 구현)하나 향후 제거됩니다 — `/start-all` 사용 권장.

## Process
`/start-all` 와 동일. 세부는 `commands/start-all.md` 참조.
