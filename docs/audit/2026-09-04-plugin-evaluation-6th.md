# specops-ko 종합 평가 6회차 (2026-09-04, v1.92.0)

> **성격**: 저자가 자기 도구를 동일 루브릭으로 반복 측정한 델타 평가. 6회차.
> **원본**: `.specops/audit/plugin-evaluation-20260904.md` (로컬 전용)
> **읽는 법**: 루브릭(5축·고정 가중)을 바꾸면 델타가 죽는다. 이어서 평가한다면 축·가중을 그대로 쓰라.

---

> 07-13 6.3(v1.44.0) · 07-14 6.7(v1.46.0) · 07-21 6.7(v1.54.0) · 07-23 6.8(v1.58.0) · 09-02 7.3(v1.91.0)에 이은 **6회차**.
> 구간: v1.91.0 → v1.92.0, **10커밋 · 1릴리즈 · 5 PR · 2일 · 955 삽입 / 83 삭제**.
> **구간이 2일이다.** 대부분의 축은 물리적으로 움직일 수 없다. 이 회차의 값어치는 델타가 아니라
> **5회차 계측의 검산**에 있고, 실제로 **두 곳이 틀렸다**(양방향 각 1건).

## 총점: **7.3 / 10** (±0 — 구성이 바뀌었고 합이 같다)

**5회차 값을 먼저 재진술한다.** 오류 2건이 부호가 반대라, 한쪽만 정정하면 새 발견이 두 번 계상된다.
정정은 **5회차 baseline 에 반영**하고, 이번 구간의 이동은 그 위에서만 센다.

| 축 | 가중 | 07-23 | 09-02 (원) | **09-02 (재진술)** | **09-04** | 이동 |
|---|---|---|---|---|---|---|
| 기계적 완성도 | 0.20 | 8 | 8.5 | 8.5 | **8.5** | — (2일, 이동 근거 없음) |
| 실사용 시 가치 | 0.25 | 8 | 9 | **8.5** ← 완주 술어 과대 | **8.5** | — |
| 경제성·비례성 | 0.20 | 4.5 | 5 | 5 | **5** | — (2일, 판단 불가) |
| 강제력(이빨) | 0.20 | 6.5 | 7 | **7.5** ← 이빨 실재 | **7** | ▼ −0.5 (외부 완주 69% 게이트 무기록) |
| 온보딩·적합도 | 0.15 | 6 | 6.5 | 6.5 | **7** | ▲ +0.5 (LICENSE) |

- 재진술한 5회차 합 = 8.5·0.20 + 8.5·0.25 + 5·0.20 + 7.5·0.20 + 6.5·0.15 = **7.300** (원 7.3 과 우연히 일치 — 두 오류가 상쇄됐다는 뜻이다)
- 6회차 합 = 8.5·0.20 + 8.5·0.25 + 5·0.20 + 7·0.20 + 7·0.15 = **7.275 ≈ 7.3**

**각 사실은 정확히 한 번만 계상된다** — 이빨 실재는 5회차 baseline 을 올리고(가산 아님),
게이트 무기록 69% 는 6회차에서 강제력을 내리고, LICENSE 는 6회차에서 온보딩을 올린다.

---

## 핵심 서사 — 자기 감사 도구가 자기 트리만 봤다

5회차의 강제력·가치 두 축은 같은 한 줄에서 나왔다:

```bash
bash scripts/skip-tracker.sh     # ← 인자 없음
```

이 스크립트는 `[.specops 경로]` 인자를 받고, **생략하면 cwd 의 `.specops` 를 본다**(`scripts/skip-tracker.sh:3`).
5회차는 specops-ko repo 안에서 이걸 돌리고 그 수치(`integration total=37`)로 **외부 63건 완주의 품질**을
논했다. 지금 자기 tree 를 다시 재면 `total=40` 이다 — 2일치만큼 늘었다. **5회차 표는 처음부터 self-dogfood 였다.**

외부를 실제로 재면 결론이 두 방향으로 갈라진다.

| | 5회차가 말한 것 | 6회차 실측 |
|---|---|---|
| bare SKIP | "18건, advisory 무이빨, 3구간 방치" | **외부 5 repo 전부 bare=0.** 이빨은 08-29 에 이미 달렸다 |
| 완주의 질 | "63건 중 상당수가 3단계를 SKIP 한 완주" | **SKIP 이 아니라 무기록.** 외부 완주 65건 중 **45건(69%)이 세 게이트 판정 자체가 없다** |

즉 **5회차는 강제력을 과소평가하고 가치를 과대평가했다.** 부호가 반대인 두 오류가 총점에서 상쇄돼
7.3 이 그대로 나온 것이지, 아무 일도 없었던 게 아니다.

---

## 축별 근거 (실측, 2026-09-04)

### 강제력 7(→ 재진술 7.5) → 7 — 이빨은 실재했다. 그런데 문이 아니라 벽에 달려 있다

**(1) 5회차의 "무이빨" 진단은 틀렸다 — 그리고 이 구간의 성과가 아니다**

`scripts/_internal/release-ready.sh:154-175` 는 `skip_cite=BARE` 를 만나면 `ready=0` → `RELEASE_READY: NOT_READY`
로 **하드 차단**한다. 이건 **2026-08-29**(FID `20260829-bare-skip-teeth`) 에 들어갔다 — **5회차가 평가한
구간 안**이다. 5회차는 자기 구간에 머지된 이빨을 못 보고 그것을 −0.5 상쇄 근거로 썼다.

따라서 **이번 구간의 가산으로 처리하지 않는다.** 이건 이번 구간의 개선이 아니라 5회차 baseline 이
낮게 찍혔다는 뜻이다. **5회차를 7 → 7.5 로 재진술**하고, 6회차 이동은 그 위에서만 센다.

**(2) 이빨은 실제로 문다 — 되돌려-관찰 대신 시계열로 확인**

이빨 이후(08-30~09-03) 생성된 self FID 9건 · 게이트 판정 27건:

| FID | security | integration | performance |
|---|---|---|---|
| 20260830-metalayer-teeth | PASS | PASS | SKIP/**CITED** |
| 20260830-silent-failure-surfacing | PASS | SKIP/**CITED** | SKIP/**CITED** |
| 20260831-matrix-pattern-lint | PASS | PASS | SKIP/**CITED** |
| 20260831-propagation-contract-record | PASS | PASS | SKIP/**CITED** |
| 20260902-dag-yaml-header-free | PASS | PASS | SKIP/**CITED** |
| 20260902-governance-lib-size-exception | SKIP/**CITED** | SKIP/**CITED** | SKIP/**CITED** |
| 20260902-runner-anchor-env-prefix | PASS | PASS | SKIP/**CITED** |
| 20260903-metrics-dead-fields | PASS | PASS | SKIP/**CITED** |
| 20260903-runner-whitelist-prefix | PASS | PASS | SKIP/**CITED** |

**bare 0건 / 27.**

bare 18건의 정확한 소재는 이렇다 — **전부 self repo** 이고, **`20260829-greet-cli-e2e` 3건이 포함된다**
(이빨이 머지된 바로 그 세션의 e2e fixture 다. fixture 실행이 stub 수정보다 앞섰다). 나머지 15건은
`20260709` ~ `20260827` FID 다. **"전부 08-29 이전"이 아니다** — 재확인 명령은
`source scripts/skip-tracker.sh; skip::cite_status <evidence.md> <gate>` 다.

**그래도 18 은 동결분이 맞다 — 주장이 아니라 확인했다.** 18 중 6건이 e2e fixture(`20260709`·`20260829`
두 세대)라 `/e2e-test` 를 다시 돌리면 재생성될 수 있다. 그런데 현재 stub 은 인용을 박는다 —
`skills/e2e-test-ko/SKILL.md:505·517·529` 가 `**근거**: §범위 L12-15` · `§NFR L50-55` 를 주입한다.
반면 `20260829` 실물 evidence 의 근거 줄에는 `L<n>` 토큰이 없다(`**근거**: greet-cli fixture — §범위: …`).
**stub 은 이미 고쳐졌고 재고만 남았다.** 소급 적용이 없으므로 18 은 줄지도 늘지도 않는다.

5회차가 "15 → 18 악화"라고 읽은 그 수는 악화 추세가 아니라 **동결된 과거 재고**다.

> **덧붙여 15 → 18 은 애초에 비교 불가였다.** 이빨 커밋 메시지가 스스로 적었다 —
> *"skip::cite_status 가 헤더 겸용 형태(`## /gate SKIP`)에서 헤더 한 줄만 보고 BARE 로 확정해 … 정직한
> 인용을 bare 로 세고 있었다. advisory 였을 때는 아무도 몰랐고, 하드로 올리는 순간 자기 fixture 6건이
> 깨져 드러났다."* v1.82.0 의 15 는 **버그 있는 계수**, 09-02 의 18 은 **고친 계수**다.
> **5회차는 자기가 다음 평가자에게 남긴 함정("155/155 를 103/103 과 빼지 마라")에 자기가 빠졌다.**

**(3) 그런데 이빨이 달린 자리가 문제다 — SKIP 이 아니라 무기록이 진짜 구멍이다**

`release-ready.sh` 는 **PR 게이트**에 있다. 세 판정이 모두 존재하는 유일한 순간이라 거기 단 것은 옳다
(스크립트 주석이 근거를 적어뒀다). 문제는 **판정이 아예 없는 경로는 PR 게이트에 도달하지 않는다**는 것이다.

외부 5 repo 실측 (`skip-tracker.sh <path>` + evidence 섹션 스캔):

| repo | 완주 | 게이트 판정 보유 | security | integration | performance |
|---|---|---|---|---|---|
| Argus | 32 | **5 (16%)** | P5/S0/**M27** | P4/S1/**M27** | P2/S3/**M27** |
| andyko-company | 13 | **0** | M13 | M13 | M13 |
| gobiseo | 8 | 8 (100%) | P8 | P8 | P2/S6 |
| Bolgflow | 7 | 7 (100%) | P7 | P7 | P7 |
| ssl-portal | 4 | **0** | M4 | M4 | M4 |
| if-auto | 1 | 0 | M1 | M1 | M1 |
| **외부 합** | **65** | **20 (31%)** | | | |

`M` = MISSING(섹션 자체 부재). **bare=0 인 이유는 SKIP 을 정직하게 인용해서가 아니라, SKIP 조차 안 적혀
있어서**인 repo 가 셋이다. Argus 완주 32건 중 27건의 evidence.md 는 `## run-verification.sh` 스탬프만
반복하고 게이트 섹션이 0개다(예: `20260729-backend-common-core`).

**원인은 이미 진단돼 있다** — `docs/case-studies/2026-08-27-argus-batch-stall.md` 가
*"batch 모드가 마지막 관문 직전에서 죽고, 그 사실이 조용히 감춰진다"* 라고 적었다. `record-batch-gate.sh`
(전파 장치)는 **08-06** 에 들어갔는데, **08-25~26 Argus batch 13 FID 는 게이트 섹션이 0개**다. 전파기가
있어도 batch 가 그 지점까지 못 갔다. 그리고 **09-03 `20260903-us-daily-candles` 도 여전히 0개다** —
과거형 결함이 아니다.

**(−) 이 구간의 외부 우회 스파이크**: Argus `20260902-us-universe-sync` 한 FID 에서 `BYPASS-ENV` **8건**,
`20260902-smart-money-market-neutral` 4건. 5회차가 "Argus 40 FID 에 BYPASS 5회"라고 쓴 뒤 **2일 만에 12건**이
추가됐다. 표본 사유는 정당해 보인다(`session-progress 기록 전용 커밋 — 코드 변경 0`, `main 동기화 머지`)
— 다만 *"FID 전체 verify 는 T3 미착수(T1 yfinance IP 429)로 구조적 FAIL"* 한 건은 **외부 요인이 verify 를
구조적으로 불가능하게 만들 때 BYPASS 말고는 길이 없다**는 뜻이다. 5회차가 봉합했다고 본 false-block 계열의
잔여형이다.

전 repo friction 총계 **920** · `BYPASS-ENV` **72**(self 40 = **56%**).

**5회차의 51 과 대조하면 두 가지가 동시에 나온다** — ts 로 갈랐다:

| 구간 | `BYPASS-ENV` |
|---|---|
| ~2026-09-01 (5회차가 셌어야 할 모집단) | **58** |
| 2026-09-02 ~ 09-04 (이번 구간) | **14** (Argus 2 FID 가 12) |
| 합 | **72** |

- 5회차는 같은 모집단을 **51** 로 셌다. **최소 7건 누락** — 상한 8호(자기 감사 도구 스코프)의 또 다른 발현이다.
- 그리고 **2일에 14건은 실제 스파이크**다. "외부 스캔이 늘어 희석됐다"는 설명은 성립하지 않는다.
  self 비율이 78% → 56% 로 내려간 것은 self 가 줄어서가 아니라(40 불변) **외부가 2일 만에 늘어서**다.

**±0 인 이유**: 이빨 실재(+) ↔ 외부 완주 69%가 게이트 무기록(−). 전자는 이미 5회차 구간의 사실이고,
후자는 5회차가 놓친 더 큰 구멍이다. 상쇄한다.

### 실사용 가치 9 → 8.5 — 열화가 아니라 **"완주"라는 술어가 약했다**

FID 실측 (5회차와 **동일 버킷 정의** — 외부 = specops-ko 밖 · `OLD/*` 제외):

| | 09-02 | 09-04 | 델타 |
|---|---|---|---|
| 외부 FID | 95 | **97** | +2 |
| 외부 완주 | 63 | **65** | +2 |
| 외부 신규(≥07-24) 완주 | 58 | **60** | +2 |
| self dogfood FID | 69 | **72** | +3 |

**2일치 노이즈다. 이걸로는 축이 못 움직인다.** 축을 움직인 건 다른 것이다 —

5회차의 8.5 → 9 는 **"외부 완주 26% → 66%"** 위에 섰고, 완주 판정 기준은 `spec·plan·tasks·evidence`
**4파일 존재**였다. 그런데 위 표대로 **그 65건 중 45건은 lifecycle 9단계 중 마지막 3단계의 흔적이 없다.**
4파일은 verify(7단계)까지의 산출물이라, **완주 술어가 security·integration·performance 를 아예 검사하지
않는다.** 5회차는 이 흠을 알았지만 "3단계를 SKIP 한 완주"로 완화해 적었고(그것도 self 수치로), 실제는
"3단계를 **기록조차 안 한** 완주"다.

**그래도 8.5 아래로는 안 내린다**: 도구는 실제로 쓰이고 있다. 15 repo · 외부 97 FID · Argus 176커밋 ·
gobiseo·Bolgflow 는 게이트 판정 보유율 100% 로 **9단계를 끝까지 도는 repo 가 실존한다**. 문제는 도구가
안 쓰이는 게 아니라 **완주 계수기가 관대하다**는 것이다.

**상한 (변동 없음)**: **3자 사용자 0명 — 6회 연속.** star 0 · fork 0 · issue 0 · watcher 0 (`gh repo view` 실측).

### 기계 8.5 → 8.5 — 2일, 이동 근거 없음

| 계측 | 09-02 | 09-04 |
|---|---|---|
| `run-all` | 155/155 | **155/155 스위트 · VERIFY: PASS** |
| `validate-structure` | 18/18 | **19/19 ✅ FAIL 0** |
| `governance/test-rules` | 88/88 | **88/88** |
| `parse-dag` | 26/26 | **33/33** (#25 로 +7) |

- `parse-dag` +7 · `validate-structure` +1 은 **이 구간 신설 검사**가 맞다(DAG 헤더 비의존 회귀 5 fixture 포함).
  그러나 5회차가 세운 원칙 — *"자체테스트 증가는 상향 근거가 아니다"* — 을 그대로 적용해 **상향하지 않는다.**
- 스위트 수 155 는 불변이다. **계기 변경 없음** — 5회차의 155 와 직접 비교 가능한 유일한 수다.

### 경제성 5 → 5 — 2일로는 판정 불가

구간 총량: **955 삽입 / 83 삭제 · 31 파일.**

| 분류 | 라인 | 내역 |
|---|---|---|
| **A (신규 능력)** | **0** | 이 구간 신규 기능 없음 |
| **B (마찰 감소)** | ~71 + 테스트 | `parse-dag.sh` 45(#25) · `run-verification.sh` 10(#26) · `governance-lib.sh` 16 |
| **C (자기유지)** | ~880 (92%) | `docs/audit/` 공개 389 · 테스트 fixture·스위트 ~330 · CHANGELOG 51 · README 33 · LICENSE 21 |

- **A=0 이지만 감점하지 않는다.** 2일이다. 5회차 처방 4번("게이트를 검사하는 게이트를 한 구간 쉬어라")에
  대해 **방향은 지켰다** — 이 구간의 코드 변경 2건(#25 DAG 헤더 · #26 러너 whitelist)은 메타 게이트가
  아니라 **하류 사용자가 실제로 밟는 경로**의 결함이고, 그건 개선제안서 1·2순위 그 자체다.
- **삭제 실적**: 죽은 계측 스키마 6필드 제거(#29, `schema_version` 2). 개선제안 5순위 이행 — "채우든지
  지우든지" 중 **지우는 쪽**을 골랐다. 정직한 선택이다.
- **커밋 세리머니**: 10커밋 중 릴리즈 chore 1 + changelog docs 1 = **2건(20%)**. 5회차의 29% 보다 낮으나
  n=10 이라 의미 없다.

### 온보딩 6.5 → 7 — 자물쇠를 뺐다

- **LICENSE MIT 추가** (#28, `a623937`). `gh repo view` → `licenseInfo.key: "mit"`. 5회차가 *"5회 연속
  미조치 항목 중 유일하게 비용이 거의 0이면서 축을 실제로 움직이는 것"* 이라고 지목한 바로 그 항목이고,
  **공개 저장소 + 무라이선스 = 전권 유보** 상태가 해소됐다. 되돌릴 수 없는 상태 변화라 2일 구간에도 계상한다.
- **평가·제안 문서 공개** (#27) — `docs/audit/` 3파일 389줄. 도입 검토자가 *"이 도구가 자기 결함을 어떻게
  다루는가"* 를 clone 만으로 볼 수 있다. `.specops/` 로컬 전용 정책의 의도적 예외로 규약까지 명문화했다.
- **(−) 7.5 를 주지 않는 이유**: 5회차가 사전 등록한 상향 트리거는 **"저자가 아닌 사람의 진입 1건"** 이었고
  LICENSE 는 그 **전제**였지 트리거가 아니다. star·fork·issue·watcher **전부 0** — 트리거는 **발화하지 않았다.**
  MIT 하나로 7.5 를 주면 사전 등록의 의미가 없다.
- 그대로인 것: 한국어 전용 · 의존 marketplace 선행 등록 필수(`ui-ux-pro-max-skill`).

---

## 사전 등록 트리거 판정 (5회차가 등록한 2건)

| 트리거 | 조건 | 판정 |
|---|---|---|
| **하향** — 강제력 7 → 6.5 | `bare` SKIP > 0 **AND** advisory 무이빨 | **미발화.** 첫 조건은 참(18건, 단 전부 08-29 이전 동결분), **둘째 조건이 거짓** — `release-ready.sh` 가 하드 차단한다. 발화하지 않은 이유가 "고쳐서"가 아니라 **"5회차가 잘못 봐서"** 임을 명시한다 |
| **상향** — 온보딩 6.5 → 7.5 | 저자 아닌 사람의 진입 1건 | **미발화.** star 0 · fork 0 · issue 0 · watcher 0 · 외부 `.specops` 전부 저자 소유 |

---

## 잔여 결함 상태

| 결함 | 상태 |
|---|---|
| 상한 1호 (의도적 날조) | 열림 (WON'T-FIX, 위협모델=환각) |
| 상한 4호 (SKIP advisory 무이빨) | **철회** — 전제가 틀렸다(PR 게이트 하드 차단 실재) |
| 상한 5호 (self-repo 우회 편중) | 열림 · 78% → 56% — **희석이 아니라 외부 증가**(self 40 불변 · 외부 2일간 +14, Argus 12) |
| **상한 6호(신규) — 완주 술어가 마지막 3단계를 안 본다** | **열림** · 외부 완주 65건 중 45건(69%) 게이트 무기록 |
| **상한 7호(신규) — batch 가 마지막 관문 전에 죽는다** | **열림 · 2구간 방치** · 08-27 사례연구가 진단, 09-03 FID 도 동일 증상 |
| **상한 8호(신규) — 자기 감사 도구의 기본 스코프가 cwd** | **열림** · 5회차의 두 오판정을 만든 직접 원인 |
| LICENSE 부재 | **fix** (#28) |
| 죽은 계측 스키마 | **fix** (#29, schema_version 2) |
| DAG 산문 헤더(제안 1순위) | **fix** (#25) |
| 러너 whitelist 접두(제안 2순위) | **fix** (#26) |
| R-1 이 팀메이트 서브에이전트 러너 실행을 못 봄 | 열림 (09-03 신규 후보, 미조치) |
| 3자 진입 | 0건 (6회 연속) |

---

## 다음 사이클 처방

1. **완주 계수기를 고쳐라 (최우선).** `spec·plan·tasks·evidence` 4파일 존재는 완주가 아니다 —
   그 정의로 센 65건 중 45건이 마지막 3단계를 안 돌았다. 세 게이트 판정 보유를 완주 조건에 넣든가,
   아니면 **"완주"라고 부르지 마라**(`verify 도달` 이 정확한 이름이다). 이 도구의 최대 실적 지표가
   자기 lifecycle 의 3분의 1을 안 보고 있다.
2. **batch stall 을 봉합하라 (상한 7호).** `record-batch-gate.sh` 는 08-06 부터 있는데 Argus batch
   13 FID 의 게이트 섹션이 0개다. 전파기가 아니라 **batch 가 거기 도달하지 못하는 것**이 문제다.
   08-27 사례연구가 이미 원인을 적었고 09-03 에도 같은 증상이 재현된다.
3. **`skip-tracker` 에 전-repo 스캔 모드를 붙여라 (상한 8호).** 인자 없이 돌면 cwd 만 본다. 자기 감사
   도구가 자기 트리만 보는 구조가 5회차의 오판정 두 건을 만들었다. `--all` 이든 환경변수든, **외부
   repo 를 세는 것이 기본**이어야 자기 도구를 자기가 속이지 않는다.
4. **다음 회차는 구간을 충분히 벌려라.** 2일 재평가는 검산에는 유용했지만 델타로는 무의미하다.
   최소 2주 · 20커밋 이상 쌓인 뒤에 재라.
5. **3자 1명.** 6회 연속 0. 이제 LICENSE 도 있다. 남은 건 저자가 아닌 사람이 `/doctor` 를 통과하는 장면 1회다.

## 사전 등록 — 7회차 트리거 (상향·하향 대칭)

- **하향**: **외부 완주의 게이트 판정 보유율이 다음 평가에서도 50% 미만이면 강제력을 7 → 6.5 로 내린다.**
  현재 31%(20/65). 이번 회차에 이미 −0.5 를 부과했으므로 **2연속 방치에만** 다시 부과한다(같은 사실로
  매회 깎지 않는다). 처방 1·2 를 둘 다 안 하면 "완주 N건"이라는 이 도구의 대표 지표가 계속 부풀려진 채로
  쌓인다 — 5회차가 자기 계기에 속은 것과 같은 병이다.
- **상향**: **저자가 아닌 사람의 진입 1건**(star·fork·issue·PR·외부 3자 소유 `.specops` 중 무엇이든)이
  관측되면 온보딩을 7 → 8 로 올린다. 6회 연속 0인 축이라 1건이 곧 질적 전환이다. (5회차 트리거를
  baseline 만 옮겨 승계한다.)

## 함정 (다음 평가자에게)

- **인자 없는 측정 명령을 믿지 마라.** 이 회차가 5회차에서 발견한 오류 2건은 전부 `skip-tracker.sh` 를
  **경로 없이** 돌린 데서 나왔다. 스크립트 usage 줄을 읽고, 외부 repo 경로를 명시해서 다시 돌려라.
- **`MISSING` 과 `SKIP` 을 같은 칸에 넣지 마라.** SKIP 은 "봤고 해당 없다"이고 MISSING 은 "안 봤다"다.
  bare=0 이 좋은 신호로 보이는 repo 중 셋은 **SKIP 자체가 없어서** 0 이었다.
- **한 사실로 두 번 점수 주지 마라.** 이빨(08-29)은 5회차 구간의 사실이다. 5회차가 못 본 것을 6회차가
  가산하면 시리즈가 무너진다 — baseline 재진술로 처리했다.
- **총점 불변을 "정체"로 읽지 마라.** 재진술한 5회차 7.300 위에서 **−0.5(강제력: 외부 게이트 무기록)
  +0.5(온보딩: LICENSE)** 를 더한 값이 7.275 다. 두 오류 정정은 baseline 쪽에 들어갔다.
  구성이 바뀌었고, 새 상한이 3개 열렸다.
